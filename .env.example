# Gazelle XDStarClient Docker Environment Configuration
# Copy this file to .env and customize as needed

# PostgreSQL Database Configuration
POSTGRES_PASSWORD=gazelle123
# POSTGRES_HOST=postgres (default, don't change unless using external DB)
# POSTGRES_PORT=5432 (default)
# POSTGRES_DB=xdstar-client (default)
# POSTGRES_USER=gazelle (default)

# JBoss Admin Console Credentials
JBOSS_ADMIN_USER=admin
JBOSS_ADMIN_PASSWORD=admin123

# Ports (change if you need different port mappings)
# POSTGRES_EXTERNAL_PORT=5432
# JBOSS_HTTP_PORT=8080
# JBOSS_ADMIN_PORT=9990
